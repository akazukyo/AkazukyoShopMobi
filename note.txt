
e.target.id 
e.target.className : Lưu ý check cái này bắt buộc phải check rõ toàn bộ class
e.target.classList : check xem toàn bộ thành phần bảo gồm class và value của vùng chọn.
e.target.classList.contains("num1") : Check xem vùng chọn có class đó không? không cần phải check toàn bộ
e.target.getAttribute("src"): dùng để lấy src của ảnh, cũng có thể dùng để lấy một số thành phần khác
(parent).children.length: dùng để đếm số lượng phần tử con có trong một div parent


:nth-last-child(): Muốn sử dụng để lấy thì phải thực hiện các input liền kề nhau.

